# Plantilla de configuración de variables de entorno
# Copia este archivo a .env y .env.production según el entorno

# Configuración del entorno
NODE_ENV=development

# Configuración de la API
# Puerto donde se ejecuta la fake API/JSON Server
API_PORT=3000
# URL base de la API (se construye automáticamente en desarrollo usando API_PORT)
API_BASE_URL=http://localhost:3000
# Timeout para las requests de API en milisegundos
API_TIMEOUT=10000

# Configuración de la aplicación
APP_NAME=PlayMatch
APP_VERSION=1.0.0

# NOTAS:
# - En desarrollo, la URL de la API se construye automáticamente usando API_PORT
# - En producción, usa API_BASE_URL directamente
# - El ConfigService maneja automáticamente la selección del entorno
